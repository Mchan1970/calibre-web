# EPUB 阅读器行距、字距设置最终合并方案

状态：间距功能已存在实现，后续调度修复及完整验收仍需完成。本文保留为间距算法、特殊内容保护及测试的背景参考；本轮唯一主方案为 `EPUB_READER_SETTINGS_PLAN.md`，存储规则、界面及实施顺序以主方案为准。

已合并 Claude 原方案的界面、按书持久化和固定版式处理，以及后续技术核查与验收力度调整。`~/.claude/plans/reactive-orbiting-summit.md` 作为历史讨论稿保留，不再作为实现依据；后续跨模块设计变更统一更新主方案，本文不再作为并行实施方案。

## 1. 目标与范围

新增行距和字距调整，以及独立的“覆盖书内行距/字距”开关。保留原书默认排版，允许用户逐项选择覆盖；调整必须可逆、按书保存，并保护阅读位置。

本轮不增加字体文件、字体目录扫描、滚轮翻页或段落间距设置，不改变 PDF 等其他阅读器。保持已有字体选择、字体覆盖及 PageUp/PageDown 行为。

## 2. 已确认的实现基础与待验证事项

- 当前字体设置通过直接修改元素的行内 font-family 实现；以 document 为 WeakMap 键记录原值和优先级，恢复后再应用。普通模式只处理 body；强制模式覆盖后代，同时锁定受保护内容原本的计算字体。
- 新章节使用 rendition.hooks.content 应用设置。此机制可共享，但不应直接复制字体覆盖代码三份。
- 当前打包的 epub.js 在支持 ResizeObserver 时观察文档根元素，否则使用定时尺寸检查。源码虽有 mutationObservers()，但不是上述初始化分支的自动退化路径。
- resizeCheck() 在测得尺寸变化后发出 CONTENTS.RESIZE。存在尺寸监听不等于任何样式变化都会可靠触发分页更新；分页、溢出、位置恢复必须用真实浏览器验证。
- rendition.layout.name 可表示 pre-paginated；section.reconcileLayoutSettings() 能解析章节属性并合并基础布局设置。方法存在只证明有解析能力，不能据此保证实际渲染已正确应用章节级布局。实现前需追踪其调用和渲染视图使用的布局。
- 无单位 line-height 会按元素字号计算，但行框还受上下标、vertical-align、混合字号、图片等影响，不能承诺消除行距不均。
- letter-spacing 的 em 值继承时传递计算后的长度；若逐元素直接设置 em，则分别按各元素字号计算。普通模式与强制模式应按各自行为验收。

## 3. 界面与数值语义

Settings 新增独立 Spacing 分组，沿用现有步进器、说明文字和间距样式。

| 属性 | 自定义范围 | 步进 | 默认状态 |
| --- | --- | --- | --- |
| 行距 line-height | 1.2～2.2，无单位倍数 | 0.1 | 原书 |
| 字距 letter-spacing | 0～0.2em | 0.02em | 原书 |

- 每项独立显示“原书”或数值，并提供独立的“恢复原书”按钮。
- “原书”在数据中使用 null，表示不覆盖。字距 0 是有效自定义值，不等同于原书；不得用真假判断混淆二者。
- 从“原书”首次点击加/减时，行距进入 1.6，字距进入 0.02em；这是自定义起点，不声称是从原书测得的统一数值。之后按步进增减，达到边界禁用对应按钮。
- 数值用整数步数计算后转换，避免浮点误差显示。
- 开关默认关闭。关闭时自定义属性仅作用于 body，允许正文继承；开启时覆盖符合条件的正文元素。关闭不等于恢复原书，恢复原书须将该属性复位。
- 两项均为原书时，开关禁用且显示未勾选，但保留此前保存的强制偏好；再次自定义任一项时恢复该偏好。
- 提示：“所设间距未生效时可开启。可能影响原书排版。”
- 控件有明确 label、单位、可访问名称和 disabled 状态，按钮组与覆盖开关保留垂直间距。

## 4. 保存与状态

- 行距、字距和强制偏好全部按书保存在 localStorage；沿用现有 bookUrl 作为书籍范围，使用独立且带版本的 spacing 键，避免影响字体及阅读位置数据。
- 每本书的值形如 {version: 1, lineHeight: null, letterSpacing: null, force: false}。
- 读取时校验版本、类型、范围和步进；损坏或不支持的值回退为原书，不让异常中断阅读器初始化。
- 存储不可用时，本次会话仍可调整；不得让存储异常阻断样式应用。无法持久保存时提供简短状态提示。
- 不跨书传播，不因进入固定版式章节删除偏好。字体的既有保存规则保持不变。

## 5. 覆盖与还原机制

共享原值记录、恢复和章节生命周期机制，但 font-family、line-height、letter-spacing 分别保留自己的目标值、强制状态和保护策略。

每次更新执行：

1. 取当前设置及每个文档的布局资格，计算该文档的有效属性集合。
2. 恢复上一次修改的受管属性，避免把上次覆盖结果当作原书样式。
3. 在写入任何祖先样式之前，统一读取需要保护的计算值；集中读、集中写，避免逐元素交替读写引起反复布局。
4. 应用完整的当前状态。普通模式仅覆盖 body；强制模式向符合条件的正文元素设置行内 !important，以处理书内同属性行内 !important。
5. 未自定义的属性不写入。单项复位不影响其他属性；固定版式只禁用间距，不改变字体设置。

以 document 为 WeakMap 键，记录每个实际修改的元素、属性原值、优先级和原先是否存在。恢复必须通过浏览器 CSSOM 测试，特别覆盖 font 简写中包含 line-height 的情况；若逐属性记录不能准确恢复简写语义，实施时补充相关简写快照，不能直接覆盖整份 style 从而抹掉阅读器的其他布局修改。

共享恢复机制是降低属性交互风险的设计选择，不代表独立操作 font-family 与 line-height 必然冲突。是否需要额外的简写快照，以真实 CSSOM 恢复测试为依据，不预先引入无必要的复杂实现。

对相同状态的重复通知应直接跳过。每个 rendition 只注册一次内容钩子，不随着开关切换重复注册。不得用空 catch 隐藏覆盖失败。

## 6. 特殊内容保护

- pre、code、kbd、samp、tt、math、svg 及其后代保护行距和字距；仅跳过子元素不能阻止父级继承污染，需采用先记录原样再隔离继承的策略。
- 行距额外保护 sup、sub、明确声明 epub:type="noteref" 的脚注标记及其后代，避免破坏原书刻意压低的行高。普通混合字号 span 仍需测试，不能承诺一切行框等高。
- 不改变 margin、padding、vertical-align、字体大小、容器高度或图片尺寸；不将所有 blockquote 或诗歌内容自动排除。
- 计算行高可能为 px。保护值应从已恢复的文档重新采样；字号变化时重新应用间距保护，避免永久锁住旧字号对应的像素行高。验证受保护区域的字号联动。
- 使用按属性定义的保护策略，不要求字体与行距使用完全相同的选择器。

## 7. 固定版式与混合版式

布局状态分为“待确认”“可重排”“固定版式”，待确认时不应用间距且禁用控件，显示“正在读取书籍版式”。

- 普通书可在布局就绪后依据 rendition.layout.name 判断。
- 实现前确认当前 epub.js 的章节级布局解析和实际视图关系。每个文档在内容钩子应用间距之前必须独立判断，不以一次全局判断代替章节判断。
- 固定版式文档停止并清除本次间距覆盖。分组禁用，提示“当前内容采用固定版式，无法调整行距和字间距。”，保存值保持不变。
- relocated 用于同步用户界面；不能作为阻止覆盖固定版式文档的唯一关卡。它也不能无条件重复应用设置，以免产生事件循环。
- 多个可见文档中存在固定或未知布局时，控件保守禁用；每个文档仍遵守自身覆盖资格。
- 若本版本无法可靠分辨混合版式章节，保守禁用该混合版式书的间距设置并明确说明，不根据反复读取全局 layout.name 宣称已支持。

## 8. 分页与阅读位置

- 修改前记录当前阅读位置 CFI；快速连续操作合并为一次最新状态更新，不堆积旧的重定位请求。
- 先利用现有尺寸监听机制，验证页面是否正确重排。若不足，依据当前 epub.js 的公开布局接口补充刷新，避免修改压缩库或使用未经确认的私有 API。
- 重排完成后检查原阅读锚点是否仍在当前视口；必要时回到记录的 CFI。不得用调整前页码或像素滚动位置恢复，因为分页已经改变。
- 若用户在更新过程中主动翻页或跳转目录，旧的恢复请求不得把用户拉回原位置。
- 明确区分 EPUB locations 生成的虚拟位置数与当前版式的实际屏幕页数，不把虚拟位置计数当成重新分页正确的证据。
- 页面稳定判定应依据渲染/尺寸事件与实际布局检查，不能仅用固定延时就认定完成。长章节、延迟加载的图片和字体需实测。

## 9. 实施顺序与文件范围

1. 准备受控 EPUB 和真实书籍样本，确认布局调用链、当前分页与位置行为。
2. 提取共享样式恢复机制，先跑既有字体行为回归，再加入间距属性。
3. 增加界面、按书存储、数值校验与独立复位。
4. 接入章节布局资格检查、内容钩子、字号变化和界面状态更新。
5. 验证分页；仅在证据表明确有必要时增加布局刷新和位置恢复逻辑。
6. 完成下述必测项，简要说明测试环境、通过项和未验证项；无需独立正式验收报告。

预计修改 reader_settings.js、reading/epub.js、standard/templates/read.html、reader.css。新增文案使用项目既有翻译机制；不为此重构其他设置或修改第三方压缩脚本。

## 10. 验收矩阵

| 场景 | 必须验证的结果 |
| --- | --- |
| 无自定义设置 | 原书样式未被改动，无跨书污染 |
| 普通模式 | body 生效；自行声明样式的段落保留原样；继承段落可变化 |
| 强制模式 | 普通规则、类选择器和行内 !important 均被有效覆盖 |
| 独立属性 | 只调行距不改字距，反之亦然；字距 0 能保存并恢复 |
| 复位与切换 | 开关反复切换、单项复位、全部复位后原值及优先级恢复；包含 font 简写样本 |
| 字体共存 | 三属性组合调整互不重置；原字体 Default 和强制模式回归通过 |
| 特殊内容 | 嵌套代码、公式、SVG、上下标、脚注、混合字号和内联图片无新增异常；字号调整后保护值正确 |
| 持久化 | 真正整页刷新后恢复，新章节恢复，书 A/B 隔离，损坏/不可用存储不阻断阅读 |
| 固定版式 | 初次加载及已有保存值均不应用间距；控件禁用且说明正确 |
| 混合版式 | 章节资格正确；若引擎能力不足，明确保守禁用而不误覆盖 |
| 分页与位置 | 书中间调整、连续调整、跨章节、调整中主动跳转，不跳回开头、无空白/裁切/旧位置回拉 |
| 既有操作 | PageUp/PageDown、左右翻页、设置焦点保持可用 |

### 本轮必须完成

- 将上述矩阵作为实施检查清单，在至少一个实际桌面浏览器中验证完整阅读流程，覆盖真实书籍与受控 EPUB 样本。
- 对状态、数值校验、保存和恢复进行有针对性的逻辑测试；不要求新建完整测试框架，也不要求每项都编写自动化脚本。
- 浏览器验证必须包含覆盖、恢复原书、整页刷新、跨章节、按书隔离、固定版式禁用、分页与阅读位置，以及共享机制修改后的字体回归。
- 检查实际 iframe、最终样式、元素几何位置、可见阅读锚点和屏幕显示，不能只根据控件状态或模拟断言判断成功。
- 在实现结果说明中简要列出浏览器及版本、通过项、未验证项和已知限制即可，无需单独产出正式验收文档。

### 兼容性补充检查

- 有可用环境时，在目标移动端或另一浏览器引擎验证设置布局、触摸操作及间距调整。
- 缺少环境时注明未验证，不作为本轮完成的硬性门槛，也不宣称已通过跨浏览器验证。

模拟 DOM 或注入一个段落只能证明局部机制。若真实浏览器、刷新或固定版式样本验证受阻，应记录具体未验证项，不能将失败归因于工具后宣布完整通过。

## 11. 规范参考

- CSS 行框高度与垂直对齐：https://www.w3.org/TR/CSS21/visudet.html
- CSS 字距与计算值：https://www.w3.org/TR/CSS22/text.html

上述规范用于解释 CSS 行为；epub.js 的版本能力以项目内打包源码及实际验证结果为准。
