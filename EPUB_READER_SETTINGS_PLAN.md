# EPUB 阅读器设置与按书偏好最终方案

状态：三个页签、按书偏好和整书重置已实现；调度初始化与键盘隔离已调整，代码检查通过。按用户要求未连接浏览器，实际显示、阅读位置和兼容性由用户验证；间歇性问题未宣称已复现定位。

本文是本轮实施的唯一主方案，覆盖稳定性修复、按书存储、整书重置和三个页签。`EPUB_SPACING_PLAN.md` 降为间距算法与保护规则的背景参考；如其旧存储规则或实施顺序与本文冲突，以本文为准。

## 1. 已确认的界面方向

将设置面板整理为“文字 / 外观 / 布局”三个页签。桌面和手机采用相同页签及顺序，不为手机另做一套折叠分组导航。

- 每次打开面板默认进入“文字”。
- 点击页签只切换显示内容，不修改设置，不丢失当前值。
- 设置即时生效，不增加“保存”按钮。
- 标题、关闭按钮、页签栏始终可见；只有当前页签的内容区域滚动。
- 面板底部固定显示“恢复本书默认设置”，切换页签时位置不变。

结构示意：

```text
阅读设置                              关闭
────────────────────────────────────────
       文字       外观       布局
────────────────────────────────────────
当前页签内容                            ↑
                                        可滚动
                                        ↓
────────────────────────────────────────
          恢复本书默认设置
       保留阅读进度、书签和已读状态
```

## 2. 设置项归属

| 页签 | 设置项 | 说明 |
| --- | --- | --- |
| 文字 | 字体 | 保留默认字体与现有字体选择 |
| 文字 | 字号 | 百分比步进调整 |
| 文字 | 行距 | 保留独立“恢复原书” |
| 文字 | 字距 | 保留独立“恢复原书” |
| 文字 | 覆盖原书排版 | 默认折叠，包含两个独立覆盖开关 |
| 外观 | 阅读主题 | 保留已有预设主题 |
| 外观 | 自定义背景色 | 保留取色器与当前自定义颜色 |
| 布局 | 单栏 / 双栏 | 保留当前阅读器支持的布局选择 |
| 布局 | 页数显示 | 保留现有开关 |
| 布局 | 侧栏打开时是否重排 | 保留现有开关 |

不增加滚轮翻页设置。PageUp / PageDown 继续沿用现有行为。

## 3. 覆盖原书排版

位于“文字”页签常用控件之后，使用一个可展开的区域：

- 覆盖书内字体。
- 覆盖书内行距 / 字距。
- 说明：“所选字体或间距未生效时可开启。可能影响原书排版。”

每次打开面板默认折叠。若存在实际启用的覆盖，折叠标题显示“已启用”，展开后可明确看到是哪一项。仅保存过强制偏好、但当前处于“原书”或固定版式禁用状态时，不应误报为正在覆盖。

原书字体状态下字体覆盖禁用；行距、字距均为原书时，间距覆盖禁用。固定版式与混合版式的间距禁用和说明沿用既有方案。折叠不会改变开关值，也不会停止正在生效的覆盖。

间距覆盖开关移出原间距分组后，仍需接受相同的状态更新和禁用逻辑，不能因为 DOM 位置变化失去关联。

## 4. 恢复本书默认设置

该按钮应恢复当前 EPUB 的全部阅读偏好，而非只重置当前页签。

| 项目 | 重置结果 |
| --- | --- |
| 字体 | 原书字体，移除字体覆盖 |
| 字号 | 100% |
| 行距、字距 | 原书样式，移除间距覆盖 |
| 两个强制开关 | 关闭，并清除本书此前保存的强制偏好 |
| 主题、自定义背景色 | 阅读器默认主题，本书不再沿用先前自定义颜色 |
| 分栏、页数显示、侧栏重排 | 阅读器默认行为 |
| 阅读进度、书签、已读状态 | 保留 |

“原书”仅指书内排版属性；主题等属于阅读器界面，没有书籍自身的默认值。实现时应统一定义阅读器默认值，并核对当前初始化逻辑，不能散落多套默认值。

重置后需立即更新所有页签、当前章节和后续章节，完整刷新后仍保持默认。不得删除整份 localStorage、用户数据库记录或阅读位置缓存，也不得影响其他书。

单项“恢复原书”继续保留。它只复位对应属性，与整书重置语义不同。

## 5. 按书保存与兼容迁移（已确认）

### 产品规则

- 面板中的全部调整只影响当前 EPUB，包括字体、字号、主题、自定义颜色、分栏、页数显示、侧栏重排及两类强制覆盖。
- 没有新版本书设置时，继承已有合法偏好以兼容旧行为；旧的按书值优先于该属性的旧全局值，最后回退至阅读器默认值。
- 首次修改时，将当前有效偏好保存为本书完整快照；此后不再混读旧存档，以免恢复已经被重置的旧值。
- “恢复本书默认”保存明确的完整默认快照，不通过删除记录恢复继承。重置后重新调整仍只写本书。
- 暂不提供“设为所有书默认”，也不更新或删除历史全局偏好。其他书首次打开时仍可使用它们兼容初始化。

### 存储盘点与适配

| 来源 | 当前用途 | 处理要求 |
| --- | --- | --- |
| calibre.reader.* 全局项 | 字体、字号、主题、自定义颜色、页数显示等 | 仅作旧偏好导入源；新版操作改写本书记录 |
| 旧按书 forceFont / spacing 项 | 字体覆盖、间距数值及覆盖开关 | 首次导入时保留，已有新版快照后不再读取 |
| reader.settings 与 reader.settings.bookKey 对应的 epubjsreader:* 存档 | 侧栏重排，以及书签、位置等第三方状态 | 以运行时 bookKey 定位，不硬编码版本/域名，不整体清空 |
| 当前 rendition 的分栏状态 | 现有分栏选择没有独立持久化 | 新版纳入本书快照；重置时立即恢复当前布局 |
| ReaderSpacing.storage 等内存缓存 | 可能缓存旧键对应的值 | 统一数据入口，重置和迁移同步缓存，避免缓存覆写新值 |

### 数据与恢复顺序

- 新增按 bookUrl 区分、带版本号的完整偏好记录，建议键为 `calibre.reader.preferences.v1.<bookUrl>`。字段包括 font、fontSize、theme、customTheme、spread、showPages、sidebarReflow、forceFont、lineHeight、letterSpacing、forceSpacing；合法默认值集中定义。
- 读取顺序：合法新版完整快照 → 旧偏好导入 → 默认值。新版记录中的缺失/非法字段使用默认值，不回头导入旧偏好。未知版本或损坏数据安全回退，不阻断阅读器启动。
- 字距 0 为有效设置；行距/字距 null 表示原书。校验数值范围、步进、字体和主题枚举、布尔类型与自定义颜色格式。
- 在章节首次应用设置之前完成偏好解析。取色器初始化、字体恢复、间距模块和界面控件共用同一有效状态，避免启动阶段各自读取不同存档。
- 存储不可用时继续使用本次会话状态，并简短提示不能持久保存。

### 第三方存档与整书重置

- `reader.min.js` 会在卸载页面时将内存 `reader.settings` 写回其存档。只修改 localStorage JSON 不够，必须同步本书相关内存偏好、UI 和实际 rendition 状态。
- 侧栏重排等需适配第三方存档的字段按白名单更新，保留 bookmarks、annotations、previousLocationCfi 等阅读数据和未知字段，不清空整个 blob。
- 对第三方 `styles` 中可能重复保存的偏好也需盘点并适配，以新版快照为权威，不能让旧字号在下次启动时重新覆盖新版设置。
- 重置在一次逻辑操作中更新有效状态、持久化快照、第三方必要字段和样式，然后同步全部页签。保留当前 CFI；若重排需要恢复位置，沿用修复后的安全调度。
- 离开页面再重新打开必须仍保持默认，不能被第三方卸载保存或会话缓存写回旧值。

## 6. 响应式与键盘交互

- 三个短标题在手机上保持同一行；窄屏控件可换行，不能产生横向溢出。
- 内容区与固定底栏之间预留空间，避免末尾控件被遮挡；自定义颜色面板也不能被错误裁切。
- 页签使用 tablist / tab / tabpanel 语义，正确设置 aria-selected、aria-controls 与焦点顺序。
- 左右方向键切换页签焦点，Home / End 定位首尾；仅当前页签内容参与键盘导航。
- Esc 关闭面板并将焦点返回设置入口；面板打开时快捷键不能误触正文翻页。
- 展开覆盖区域使用可聚焦按钮与 aria-expanded；禁用原因与状态提示可被辅助技术读取。
- 保持当前视觉风格、主题适配和足够的控件间距，不引入新的前端框架。

## 7. 前置稳定性修复

### 字体恢复被位置读取阻断

已确认缺陷：`schedule()` 在安排样式更新前直接调用 `reader.rendition.currentLocation()`；阅读器尚未就绪时可能抛异常，使后续样式更新未执行。

- 样式应用必须独立于阅读锚点获取。位置不可用时跳过位置保持，仍应用最新设置。
- 初始化、book.ready、首章渲染的时序不得影响字体偏好恢复。
- 对读取位置的异常采用有边界的处理；不要用一个空 catch 吞掉整个样式流程。

### 连续调整间歇性不生效

已有一次实际浏览器观察：界面改变后 body.style.letterSpacing 仍为空，持续超过 3 秒；触发条件与根因尚未确定。不能将疑似调度竞态作为已经确认的根因。

- 检查 schedule / apply / settlePosition 的更新合并、取消、布局事件及异步重定位关系。
- 最新设置必须最终应用；取消位置恢复不得取消设置更新。
- 位置恢复不能覆盖用户后续主动翻页/目录跳转，也不能让早期异步更新覆写最新值。
- 用连续增减、切换覆盖、快速复位、跨章节和刷新进行重复实测；检查真实正文样式而非只看显示值。
- 若无法再次复现，记录观察条件、诊断结果与测试次数；不得把“未复现”写成“已定位修复”。

### 设置面板内方向键误翻页

已确认：自有 pageKeys 只保护 PageUp/PageDown；第三方 ReaderController.arrowKeys 独立监听 document 与 rendition 的左右方向键，未检查面板状态。

- 在接入页签方向键导航之前隔离该行为，面板打开时不能让左右方向键进入正文翻页逻辑。
- 不能仅依赖 preventDefault，因为第三方处理器不检查 defaultPrevented。应针对事件传播/处理入口采取有效隔离，并兼顾宿主文档和章节事件。
- 保留页签键盘导航和输入控件的默认操作，面板关闭后正文左右键、PageUp/PageDown 仍可使用。
- 优先通过应用层接入处理，避免直接修改第三方压缩脚本。

## 8. 最终实施顺序与范围

1. 先处理调度初始化缺陷与方向键冲突，调查间歇性不生效，并回归既有字体、间距和阅读位置行为。
2. 引入统一按书状态及旧数据导入，完成第三方存档/缓存适配与整书重置。
3. 将界面整理为三个页签，移动覆盖控件到折叠区，接入实际生效徽标和固定底栏。
4. 完成真实浏览器核心验收和窄屏布局检查，再交付；未验证项明确列出。

预计涉及 `read.html`、`reader.css`、`reader_settings.js`、`reader_spacing.js`、`reading/epub.js`；必要时可提取独立偏好模块。新增文案沿用翻译机制，不引入新前端框架。

## 9. 验收清单

### 稳定性与已有功能

- 保存字体后完整刷新，首章真实字体覆盖恢复；位置暂不可用不阻断设置应用。
- 连续调整、开关切换及复位后，真实正文最终样式与最新设置一致。
- 书中间调整后位置合理保留；主动翻页/目录跳转不会被旧恢复请求拉回。
- 设置面板打开时左右键及 PageUp/PageDown 不误翻页，关闭后保持原有操作。
- 固定/混合版式保护与字体、间距独立复位继续生效。

### 存储、兼容和整书重置

- 老全局偏好、旧按书间距/覆盖、第三方侧栏重排值能正确导入；新记录生成后以其为准。
- 修改书 A 的任何面板选项不改变书 B；分栏在刷新后也能恢复本书选择。
- 新版非法字段、损坏记录、不可用存储不阻断阅读器。
- 整书重置立即生效，全部页签一致；重新打开、刷新、换章节后保持默认，不重新继承旧全局偏好。
- 卸载保存后旧侧栏重排、字号等不回流；进度、书签、注释、已读状态和其他书设置保留。
- 重置后再次调整不会复活旧强制偏好；单项复位仅影响对应属性。

### 界面与可访问性

- 打开默认进入文字页签，切换不重置值；外观和布局控件正常工作。
- 覆盖折叠标题的“已启用”反映实际生效状态，不误报保存但未应用的偏好。
- 桌面和窄屏没有横向溢出，底栏不遮挡控件，取色器和内容滚动正常。
- 页签方向键、Home/End、Esc、焦点恢复与隐藏面板焦点隔离正常。

至少在一个实际桌面浏览器完成核心流程，并检查窄屏布局。逻辑测试针对状态导入、校验与重置；不要求另建大型测试框架。移动端实机或另一浏览器引擎有环境时补测，缺少环境不作为硬性门槛。

简要记录测试环境、通过项和未验证项即可，无需正式验收报告。浏览器验证受阻或偶发问题尚未定位时应如实说明，不能仅以模拟 DOM 或界面显示宣称完整通过。


## 10. 本轮实现检查结果

- 新增 reader_preferences.js 统一按书快照、旧偏好导入及第三方存档适配；间距与界面模块通过同一状态更新。
- 样式更新不再依赖 currentLocation()，同步应用最新值，位置保持单独调度。未重新复现此前一次间歇性异常，不将根因推测写成已确认结论。
- 使用 Node 内置测试运行器完成 6 项逻辑回归：迁移与隔离、第三方数据保留、非法/不可用存储、100 次快速更新、跨章节/固定版式、页签按键隔离。
- 新增及重写的 preferences/settings/spacing 模块通过 ESLint；模板渲染、唯一 ID、页签面板引用、资源路径和差异空白检查通过。
- 未启动或连接浏览器。用户需验证三个页签实际布局、字体/间距连续调整、书 A/B 设置隔离、整书重置后刷新以及阅读进度和书签保留。
